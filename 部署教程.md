# KOL Finder 完整部署教程（超详细版）

这个教程会带你从零开始，把网红挖掘网站搭起来并部署到线上。全程不需要写代码，只需要复制粘贴和点击。

预计总时间：20-30分钟。

---

## 第一部分：设置 Supabase（公共数据库）

Supabase 是一个免费的在线数据库，用来存放你们团队的网红库。设好之后，10个人搜到的博主都会存到同一个地方。

### 第1步：注册 Supabase 账号

1. 打开浏览器，访问 https://supabase.com
2. 点右上角绿色按钮 **"Start your project"**
3. 会跳转到登录页面，你有两个选择：
   - **方式A**：点 "Sign in with GitHub"（如果你有GitHub账号，之前部署Naver工具时注册的那个就行）
   - **方式B**：用邮箱注册，填邮箱地址 → 设密码 → 去邮箱点验证链接
4. 登录成功后会进入一个仪表盘页面（Dashboard）

### 第2步：创建项目

1. 进入 Dashboard 后，你会看到一个 **"New Project"** 按钮（绿色），点它
2. 填写以下信息：
   - **Organization**：如果让你选组织，选默认的就行（或点 "Create a new organization"，名字随便填比如 `my-team`）
   - **Name**：填 `kol-finder`
   - **Database Password**：随便设一个密码，比如 `Kol2026finder!`（这个密码我们实际不用，但必须填，记不记都行）
   - **Region**：下拉选择 **`Northeast Asia (Tokyo)`**（如果没有就选 `Singapore`）
   - **Pricing Plan**：选 **Free**（免费）
3. 点底部绿色按钮 **"Create new project"**
4. 等待 1-2 分钟，页面会显示项目创建进度。看到 "Your project is ready" 或进入项目主页就好了

### 第3步：创建数据表

1. 项目创建好后，看页面**左侧**有一列图标菜单栏
2. 找到一个像 **"终端/代码"** 的图标，鼠标悬停会显示 **"SQL Editor"**，点它
   - 如果找不到，也可以看左侧有没有一个 "Table Editor" 旁边有个 `</>` 图标
3. 进入 SQL Editor 后，你会看到一个空白的编辑区域（像记事本一样）
4. 把下面这一整段**完整复制**，粘贴到编辑区域里（把里面原有的内容全选删掉再粘贴）：

```sql
CREATE TABLE influencers (
  id BIGSERIAL PRIMARY KEY,
  channel_id TEXT UNIQUE NOT NULL,
  channel_name TEXT DEFAULT '',
  channel_url TEXT DEFAULT '',
  about_url TEXT DEFAULT '',
  category TEXT DEFAULT '',
  subscribers INTEGER DEFAULT 0,
  avg_views_30d INTEGER DEFAULT 0,
  view_sub_ratio REAL DEFAULT 0,
  last_upload TEXT DEFAULT '',
  score_total INTEGER DEFAULT 0,
  score_detail TEXT DEFAULT '',
  emails TEXT DEFAULT '',
  has_commercial BOOLEAN DEFAULT FALSE,
  commercial_evidence TEXT DEFAULT '',
  recent_titles TEXT DEFAULT '',
  thumbnails TEXT DEFAULT '',
  status TEXT DEFAULT '新发现',
  status_date TEXT DEFAULT '',
  discovered_by TEXT DEFAULT '',
  email_sent_date TEXT,
  notes TEXT DEFAULT '',
  added_date TEXT DEFAULT '',
  last_checked TEXT DEFAULT ''
);

ALTER TABLE influencers ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Allow all access" ON influencers
  FOR ALL USING (true) WITH CHECK (true);
```

5. 粘贴好后，点编辑区域**右下角**的绿色按钮 **"Run"**（或按键盘 `Command + Enter`）
6. 下方会出现结果：
   - 看到绿色 **"Success"** → 成功了，继续下一步
   - 看到红色 **"relation influencers already exists"** → 说明之前已经创建过了，也是成功的，继续下一步
   - 看到其他红色报错 → 截图发给我

### 第3.5步：再建一张"成员名单"表（新网站需要）

这张表用来存团队每个人的名字，这样侧边栏会出现名字下拉框，点一下就行，不用每次手打。

1. 还是刚才那个 SQL Editor，把里面的内容**全选删掉**，粘贴下面这一小段：

```sql
CREATE TABLE members (
  id BIGSERIAL PRIMARY KEY,
  name TEXT UNIQUE,
  joined_at TEXT DEFAULT ''
);

ALTER TABLE members DISABLE ROW LEVEL SECURITY;
```

2. 点右下角绿色按钮 **"Run"**
3. 看到绿色 **"Success"** → 成功；看到 "relation members already exists" → 说明建过了，都没问题

### 第4步：获取连接信息（两个字符串）

1. 看左侧菜单栏，找到最下面的 **"Settings"**（齿轮⚙️图标），点它
2. 进入 Settings 后，看左侧子菜单，找到 **"API"**，点它
3. 你会看到页面上有几个信息，我们需要两个：

**第一个：Project URL**
- 页面上写着 "Project URL"，后面跟着一个网址
- 长这样：`https://xxxxxxxx.supabase.co`（x是一串随机字母）
- 点旁边的复制图标，复制下来

**第二个：anon public key**
- 页面往下滚，找到 "Project API keys" 区域
- 里面有两个 key，我们要的是 **`anon` `public`** 那一个（不是 service_role 那个！）
- 它是一长串以 `eyJ` 开头的字符串
- 点复制图标，复制下来

4. 把这两个字符串暂时存在备忘录里，待会要填到网站中

---

## 第二部分：在自己电脑上先跑起来试试

### 第1步：确认文件在桌面

桌面上应该有一个 `kol-finder` 文件夹（我已经帮你放好了），打开确认里面有这几个文件：app.py、youtube_api.py、database.py、requirements.txt、部署教程.md

### 第2步：打开终端

1. 按键盘 `Command + 空格`（会弹出聚焦搜索）
2. 输入 **终端**（或 **Terminal**）
3. 按回车，会打开一个黑色/白色的文字窗口

### 第3步：进入文件夹

在终端里**输入下面这一行**（只输入这一行，不要加别的），然后按回车：

```
cd ~/Desktop/kol-finder
```

如果没报错（没出现红色文字），说明成功了。

> 如果报错 "No such file or directory"：说明文件夹名字或位置不对。确认文件夹确实在桌面上、名字确实叫 `kol-finder`（没有多余空格或中文）。

### 第4步：安装依赖

输入下面这一行，按回车：

```
pip3 install -r requirements.txt
```

等它跑完（会显示很多 "Requirement already satisfied" 的文字，这是正常的，说明之前已经装好了）。

> 如果报错 "pip3: command not found"：试试输入 `python3 -m pip install -r requirements.txt`
> 如果报错 "Permission denied"：输入 `pip3 install -r requirements.txt --user`

### 第5步：启动网站

输入下面这一行，按回车：

```
streamlit run app.py
```

第一次运行会问你 Email，**直接按回车跳过**（不用填）。

等几秒钟，你会看到：
- 终端里出现 "You can now view your Streamlit app in your browser"
- 浏览器**自动打开**一个新页面，地址是 `http://localhost:8501`
- 页面上显示 "KOL Finder" 标题

> 如果浏览器没自动打开：手动打开浏览器，地址栏输入 `http://localhost:8501` 回车

### 第6步：填入配置

网站打开后，看**左侧边栏**：

1. **"你的名字"**：填你的名字或昵称（比如 `Ivy`），用于标记谁挖掘的
2. **"YouTube API Key"**：填入你的 YouTube API Key（以 `AIza` 开头的一串字符）
3. **"Supabase URL"**：填入第一部分第4步复制的 Project URL
4. **"Supabase Key"**：填入第一部分第4步复制的 anon public key

填完后，如果看到绿色的 **"✅ 公共库已连接"**，恭喜，全部设置完成！

> 如果显示 "❌ 连接失败"：检查 URL 和 Key 有没有复制完整（特别是 Key 很长，容易少复制最后几个字符）

### 第7步：试一下搜索

1. 在顶部 Tab 栏点 **"🔎 搜索挖掘"**
2. 垂类选 "家居收纳"
3. 关键词输入框里输入：`자취방 꾸미기 가성비`
4. 点 **"🔍 搜索"** 按钮
5. 等几秒钟，如果下方出现频道卡片，说明一切正常！

### 关闭网站

想关掉网站时，回到终端窗口，按 `Control + C`（同时按这两个键）就停了。

---

## 第三部分：部署到线上（团队共用）

本地确认能正常搜索后，接下来把网站放到网上，这样团队所有人打开一个链接就能用，不需要在自己电脑上装任何东西。

整个过程分两大步：先把代码传到 GitHub（相当于一个"代码网盘"），然后让 Streamlit Cloud 从 GitHub 读取代码并运行。

---

### 第1步：打开 GitHub 创建仓库页面

1. 打开浏览器，访问 https://github.com/new
2. 如果你没登录，会先让你登录 GitHub 账号（就是之前部署 Naver 工具时注册的那个）
3. 登录后你会看到一个 **"Create a new repository"** 页面

### 第2步：填写仓库信息

页面上有很多选项，你只需要填/选以下几个，**其他的都不要动**：

1. **Repository name**（仓库名称）：在输入框里打字输入 `kol-finder`（全小写，中间一个横杠）
2. **Description**（描述）：可以不填，留空
3. **Public or Private**（公开还是私有）：
   - 选 **Public**（公开）
   - 为什么选公开：Streamlit Cloud 免费版只能部署公开仓库。如果选 Private 后面部署会报错
   - 不用担心代码被人看到——这个代码没有敏感信息，API Key 是每个人打开网站后自己填的，不存在代码里
4. **IMPORTANT 不要勾选的**：
   - ❌ 不要勾 "Add a README file"
   - ❌ "Add .gitignore" 下拉框保持 "None"
   - ❌ "Choose a license" 下拉框保持 "None"
5. 确认以上后，点页面最底部的绿色按钮 **"Create repository"**

> 如果报错 "name already exists"：说明你之前已经建过同名仓库了。换个名字比如 `kol-finder-v2`，或者去 github.com 你的主页找到旧仓库删掉重建。

### 第3步：上传代码文件

1. 点完 "Create repository" 后，会进入一个新页面，显示你的空仓库
2. 页面中间会有一段引导文字，你能看到几个蓝色链接，找到 **"uploading an existing file"** 这个链接，点它
   - 如果没看到这个链接（页面布局可能不同），找页面右上方有一个 **"Add file"** 按钮，点它，下拉菜单里选 **"Upload files"**
3. 进入上传页面后，你会看到一个**虚线大方框**，上面写着 "Drag files here to add them to your repository"
4. 现在打开你电脑上的 `kol-finder` 文件夹（桌面上那个）
5. 从文件夹里把以下 **4个文件** 拖进网页上的虚线框里（直接拖，像拖文件到U盘一样）：
   - `app.py`
   - `youtube_api.py`
   - `database.py`
   - `requirements.txt`
6. **不要拖** `部署教程.md`（那个是给你看的说明，不是网站需要的）
7. 拖进去后，虚线框下方会显示4个文件名（每个前面有个小图标），说明上传成功
8. 等几秒钟让文件上传完（文件小的话基本秒传）
9. 页面往下滚，找到 **"Commit changes"** 区域：
   - "Commit message" 输入框：不用改，保持默认就行
   - 直接点绿色按钮 **"Commit changes"**
10. 点完后会跳回仓库主页，你能看到4个文件列在那里，说明上传成功了

> 怎么确认上传成功：仓库页面应该显示 app.py、database.py、requirements.txt、youtube_api.py 这4个文件名。如果少了哪个，点 "Add file" → "Upload files" 再补传。

### 第4步：打开 Streamlit Cloud

1. 打开新标签页，访问 https://share.streamlit.io
2. 你会看到一个登录页面，点 **"Sign in with GitHub"** 按钮
3. 会跳转到 GitHub 授权页面，问你 "Streamlit wants to access your GitHub account"
4. 点绿色按钮 **"Authorize Streamlit"**（授权 Streamlit 读取你的仓库）
5. 授权成功后，会进入 Streamlit Cloud 的控制台页面

> 如果你之前部署 Naver 工具时已经授权过，这次会直接进去，不用再授权。

### 第5步：创建新应用

1. 进入 Streamlit Cloud 后，看页面**右上角**，有一个 **"New app"** 按钮，点它
2. 会弹出一个表单，需要填3个东西：

**Repository（仓库）**：
- 点下拉框，找到 `你的GitHub用户名/kol-finder`，选它
- 如果下拉框里找不到：点下拉框下方的 "I don't see my repository" 链接，按提示授权一下就行

**Branch（分支）**：
- 选 `main`（默认就是 main，不用改）

**Main file path（主文件路径）**：
- 在输入框里打字输入：`app.py`
- 就这四个字符，不要加别的东西

3. 三个都填好后，点底部 **"Deploy!"** 按钮

### 第6步：等待部署完成

1. 点完 Deploy 后，页面会显示部署进度，你能看到一个转圈的动画和日志输出
2. 这个过程大约需要 **2-4 分钟**（它在安装依赖、启动程序）
3. 你会看到日志里出现类似这些内容（不用看懂，知道就行）：
   - "Installing dependencies..."（在安装依赖）
   - "Collecting streamlit..."（在下载 Streamlit）
   - "Your app is now live!"（上线了！）
4. 部署成功后，页面**最上方**会显示你的网站地址，格式类似：
   `https://你的用户名-kol-finder.streamlit.app`
5. 点这个链接，能打开网站就成功了！

> **如果部署失败（显示红色错误）**：
> - 最常见原因：仓库选了 Private。解决：去 GitHub 仓库页面 → Settings → 最底部 "Danger Zone" → "Change visibility" → 改为 Public → 回 Streamlit Cloud 点 "Redeploy"
> - 第二常见：文件没传全。去 GitHub 仓库确认有4个文件
> - 如果日志里出现 "ModuleNotFoundError"：说明 requirements.txt 没传上去或内容不对
> - 其他报错：截图发给我

### 第7步：配置线上网站

1. 打开你的网站地址（`https://xxx.streamlit.app`）
2. 第一次打开可能需要等10-20秒（冷启动）
3. 页面出来后，在**左侧边栏**填入：
   - **你的名字**：比如 `Ivy`
   - **YouTube API Key**：你的那个以 `AIza` 开头的 Key
   - **Supabase URL**：之前从 Supabase 拿到的 Project URL
   - **Supabase Key**：之前拿到的 anon public key
4. 看到 **"✅ 公共库已连接"** 就OK了
5. 试一下搜索，确认能出结果

> **重要提醒**：Streamlit Cloud 如果超过一段时间没人访问，会进入"休眠"状态。下次有人打开时需要等20-30秒唤醒，这是正常的，不是坏了。

### 第8步：发给团队使用

把网站地址发给团队其他9个人。每个人第一次打开时需要填：

1. **自己的名字**（用于标记"谁挖掘的"）
2. **YouTube API Key**（两种方案任选）：
   - 方案A：共用你的 Key（简单，但大家共享每天10,000 units配额）
   - 方案B：每人自己申请一个 Key（配额各自独立，但每人要自己去 Google Cloud 创建）
3. **Supabase URL 和 Key**：所有人填**同一个**（就是你第一部分拿到的那两个字符串，直接群里发给大家复制粘贴）

填完后所有人看到的数据是**同步的**——A搜到并加入网红库的博主，B打开网红库也能看到。

### 第9步（可选）：改一个好记的网址

如果你想让网址更好记（比如改成 `kol-finder.streamlit.app`）：

1. 在 Streamlit Cloud 控制台，找到你的应用
2. 点应用名称旁边的 **"Settings"**（齿轮图标）
3. 找到 **"Custom subdomain"** 区域
4. 把默认的 `你的用户名-kol-finder` 改成你想要的名字，比如 `kol-finder`
5. 点保存，网址就变成 `https://kol-finder.streamlit.app`

> 如果名字被别人占了会提示不可用，换一个就行。

---

## 第四部分：日常使用指南

### 搜索博主

1. 打开网站 → "🔎 搜索挖掘"
2. 选垂类 → 输入关键词 → 点搜索（或点"批量搜索"跑完整个垂类）
3. 切到 "📊 搜索结果" 看卡片
4. 看缩略图判断视觉风格，看评分和数据判断质量
5. 觉得行的点 "✅ 加入网红库"，不行的点 "跳过"

### 管理网红库

1. 打开 "📁 网红库"
2. 改状态：下拉选择 新发现/已发邮件/已引入/已拒绝
3. 写备注：在备注框里写（比如"7.26已发邮件""回复说感兴趣"）
4. 复查：点 🔄 按钮检查博主是否还活跃
5. 导出：底部有"导出网红库"按钮，下载Excel备份

### 导入已有合作博主

1. 打开 "📥 批量导入"
2. 把频道链接每行一个粘贴进去，比如：
   ```
   https://www.youtube.com/channel/UCxxxxxxxx
   https://www.youtube.com/channel/UCyyyyyyyy
   ```
3. 状态选 "已引入"
4. 点 "开始导入"
5. 导入后这些人就不会再出现在搜索结果里了

### 调整筛选标准

1. 打开 "⚙️ 筛选设置"
2. 可以改：订阅量范围、评分阈值、各维度权重、去重天数
3. 改完点 "💾 保存设置"
4. 下次搜索就会用新标准

---

## 配额说明

YouTube Data API 免费额度：每天 10,000 units（每天0点自动重置）

| 操作 | 消耗 |
|------|------|
| 搜索一个关键词 | 100 units |
| 验证一个频道的全部信息 | 约 3 units |
| 一次完整搜索（搜索+验证所有候选） | 约 110-150 units |
| 一次批量搜索（7个关键词） | 约 800-1000 units |

每天大约可以：搜索 60-80 个关键词，或做 8-10 次批量搜索。

左侧边栏实时显示配额用量，快用完时会提醒。

---

## 去重规则

| 网红状态 | 搜索结果中的表现 |
|---------|----------------|
| 已引入 | 永远不再出现 |
| 已拒绝 | 30天内不出现，之后重新出现 |
| 已发邮件 | 7天内不出现，之后重新出现（方便跟进） |
| 新发现 | 7天内不出现，之后重新出现（提醒跟进） |

以上天数都可以在"筛选设置"里改。

---

## 常见问题排查

### Supabase 相关

**Q: SQL Editor 在哪里？**
A: 左侧图标栏，找一个像 `>_` 或 `</>` 的图标。如果实在找不到，在页面顶部搜索栏输入 "SQL" 也能搜到。

**Q: 报错 "relation already exists"？**
A: 说明表已经创建好了，不用管，直接去拿 URL 和 Key。

**Q: 网站显示"连接失败"？**
A: 三个可能：① URL 或 Key 复制不完整（Key 很长，注意末尾）② 复制时多了空格 ③ 确认复制的是 `anon public` 那个 key，不是 `service_role` 那个。

**Q: 数据会丢吗？**
A: 不会。数据存在 Supabase 云端，只要你不删项目就一直在。即使 Streamlit 网站重启，数据也不受影响。

### 本地运行相关

**Q: 终端里输入命令后没反应？**
A: 可能还在处理中，等一等。如果超过3分钟没动静，按 `Control + C` 取消，重新输入。

**Q: 报错 "ModuleNotFoundError: No module named 'streamlit'"？**
A: 依赖没装好。重新运行：`pip3 install -r requirements.txt`

**Q: 报错 "Address already in use"？**
A: 上次的网站没关干净。关掉所有终端窗口，重新打开一个，再运行 `streamlit run app.py`。

**Q: 怎么同时跑 Naver 比价工具和这个？**
A: 两个工具在不同文件夹里，分别打开两个终端窗口，一个跑 Naver（端口8501），另一个跑 KOL Finder 时加参数：`streamlit run app.py --server.port 8502`

### GitHub / Streamlit Cloud 相关

**Q: Streamlit Cloud 部署失败？**
A: 最常见原因是仓库选了 Private。去 GitHub 仓库 → Settings → 最底部改为 Public → 回 Streamlit Cloud 点 "Redeploy"。

**Q: 线上网站打开是空白？**
A: 等2-3分钟，Streamlit Cloud 第一次启动需要时间。如果5分钟后还是空白，点页面右上角 "Manage app" → "Reboot app"。

**Q: 团队成员打不开网址？**
A: 确认仓库是 Public 的。如果是 Public 还打不开，让同事也注册一个 Streamlit 账号（用GitHub登录就行）再打开。

**Q: 想更新代码怎么办？**
A: 去 GitHub 仓库页面，点那个文件 → 点右上角铅笔图标（Edit）→ 改完点 "Commit changes"。Streamlit Cloud 会自动检测到更新并重新部署（等2-3分钟）。或者直接找我帮你改。

---

## 文件说明

| 文件 | 作用 |
|------|------|
| app.py | 网站界面（你看到的所有页面） |
| youtube_api.py | YouTube搜索和评分逻辑 |
| database.py | 连接Supabase数据库 |
| requirements.txt | 依赖清单（安装用） |
| 部署教程.md | 就是这份文档 |
